export type Post = {
    id?: number;
    name: string;
    date: number;
    postName: string;
    view: number;
}

export type User = {
    username: string;
    password: string;
    email: string;
}